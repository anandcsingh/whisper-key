{
  "ClassName": "UserCredentials",
  "fields": [
    { "title": "Username", "type": "string" },
    { "title": "Password", "type": "string" }
  ],
  "ctr": function(){
    return this.title + ": " + this.type + ",";
  },
  "concat": function(){
    return ".concat(this." + this.title + ".toFields())"
  }
}